# Python SDK build

The authoritative contract is:

```text
backend/openapi/beeos-platform-v1.yaml
```

The `openagent` workspace generates this tree with OpenAPI Generator and
synchronizes it to `github.com/beeos-ai/sdk-python`. Generated files must not
be edited by hand in the independent repository.
