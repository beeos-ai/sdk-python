# coding: utf-8

"""
    BeeOS OpenAPI (user contract)

    The **BeeOS OpenAPI** user contract. The sole implementer is **[`services/openapi-gateway`](../services/openapi-gateway)** (`openapi-gw`, local `:8095`). SDKs (Go / TypeScript) published under [`sdks/beeos-ai-sdk-go`](../../sdks/beeos-ai-sdk-go) and [`sdks/beeos-ai-sdk`](../../sdks/beeos-ai-sdk) are generated directly from this document.  The main user Gateway (`services/gateway`, `:9080`) serves web / mobile / desktop clients and is **not** described by this document. It maintains its own route set independent of the SDK contract.  **Auth:** every operation requires either a user **JWT** or a **`oag_`** User API Key, both on `Authorization: Bearer`. Agent-level protocol endpoints (A2A JSON-RPC with `bak_`, MCP) live in [beeos-agent-integration-v1.yaml](beeos-agent-integration-v1.yaml) and are hosted by A2A Gateway / MCP Gateway — not by `openapi-gateway`.  ## Changelog  ### 1.1.0 (ADR-0022 — dual-layer message storage)  BREAKING for clients that depended on full streaming chunk replay via the persistent message log:  * `GET /api/v1/agents/{agentId}/conversations/{convId}/messages`   and `GET /api/v1/agents/{agentId}/tasks/{taskId}/messages` now   **default-filter out** ephemeral streaming chunks   (`agent_reply_delta`, `agent_thought_chunk`,   `agent_message_chunk`). Add `?include_deltas=true` to opt back in. * `latest_offset` continues to reflect the TRUE server-side max   offset across both filtered and unfiltered rows, so `since=` /   cursor-style pagination is unaffected. * SSE `/events` is **NOT** affected — live consumers continue to   receive every envelope as it happens. * Backwards-compatible recovery path: SSE `/events` may now emit   a `backfill_truncated` event frame when the client reconnects   with `Last-Event-ID` before the oldest retained chunk. SDKs   that don't recognise the frame should treat it as a hint to   resume from `oldest_redis_offset` instead (the field carrying   the smallest offset still readable on the per-channel stream). * **Clarified contract**: SSE event `offset` is strictly monotonic   per channel but **NOT guaranteed contiguous** (ADR-0022 §1.2).   Producer-side write failures may leave small holes; clients MUST   use `offset > since`, never `offset == since + 1`.  BREAKING for clients that depended on the `oag_` API key scope vocabulary:  * The per-route scope gate (`agents:read`, `agents:write`,   `tasks:read`, `tasks:write`, `files:read`, `files:write`,   `instances:read`, `instances:write`, plus the `admin:*` wildcard)   has been **removed entirely**. The 37 routes that previously   required scopes are now governed exclusively by owner-ACL — an   `oag_` User API Key inherits full access to its owner's resources. * The `403 insufficient_scope` error code is no longer emitted.   Existing callers that branched on it should fold the case into   their generic 403 / `forbidden` handler. * SDK clients that explicitly passed `scopes` to `createAPIKey`   should drop that argument. The Web / Desktop UIs no longer render   scope badges on existing keys. * Existing `oag_` keys continue to work unchanged — the owner_id   binding is preserved, and no key needs to be re-issued.  BREAKING for clients of `GET /api/v1/providers` that read the `capabilities` object on the wire:  * 7 of the 8 `capabilities` field names are now **camelCase**   (`longRunning`, `browserUse`, `codeExec`, `fileSystem`,   `customImage`, `maxDurationSec`, `costModel`) — matching this   OpenAPI contract. Previous wire emitted **snake_case**   (`long_running`, `browser_use`, …) because the server DTO bound   `capabilities` to the raw protobuf message   (`*pb.ProviderCapabilities`) whose auto-generated `json` tags   use snake_case. SDK consumers (`@beeos-ai/sdk`,   `github.com/beeos-ai/sdk-go`) generated from this spec were   **already unable** to read any `capabilities` field — the   camelCase property names produced by the generator never   matched the snake_case the server actually sent. Raw HTTP   callers that hand-coded against the old snake_case names must   migrate; SDK callers gain access to the field set for the first   time without code changes. * Note: the `device` boolean was always a single word — its wire   name (`device`) is unchanged. Only the seven multi-word   capability fields are affected by the rename. * `meta`, `id`, `name`, `description`, `version` are unchanged. * A new drift-guard (`internal/dto/contract_test.go`) now locks   the server DTO to the spec property names, so the two cannot   silently diverge again.  ### 1.0.0  Initial OpenAPI contract (ADR-001). 

    The version of the OpenAPI document: 1.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class InstanceDataDTO(BaseModel):
    """
    InstanceDataDTO
    """ # noqa: E501
    id: StrictStr
    owner_id: StrictStr = Field(alias="ownerId")
    agent_framework: StrictStr = Field(alias="agentFramework")
    provider_id: StrictStr = Field(alias="providerId")
    external_id: StrictStr = Field(alias="externalId")
    os_type: StrictStr = Field(alias="osType")
    hosting_type: StrictStr = Field(alias="hostingType")
    cloud_provider: StrictStr = Field(alias="cloudProvider")
    name: StrictStr
    model_primary: StrictStr = Field(alias="modelPrimary")
    models: List[StrictStr]
    status: StrictStr = Field(description="One of `pending` | `provisioning` | `running` | `stopped` | `terminated`. ")
    desired_status: StrictStr = Field(description="One of `running` | `stopped` | `terminated`. ", alias="desiredStatus")
    endpoint: StrictStr
    bridge_id: StrictStr = Field(alias="bridgeId")
    identity_id: StrictStr = Field(alias="identityId")
    total_run_seconds: StrictInt = Field(alias="totalRunSeconds")
    connectivity: StrictStr = Field(description="One of `unknown` | `online` | `offline`.")
    connectivity_reason: Optional[StrictStr] = Field(default=None, description="Open-string reason annotating a non-online `connectivity` value (independent of `haltReason`). Canonical values: `device_disconnected` | `device_unauthorized` | `instance_disconnected` (legacy alias `agent_disconnected` still accepted during Phase 1 rollout). Empty when connectivity is online. Lifecycle is tied to connectivity: any transition into `online` strictly clears this field. ", alias="connectivityReason")
    halt_reason: Optional[StrictStr] = Field(default=None, alias="haltReason")
    provider_config: Dict[str, Any] = Field(description="Sanitized provider config (apiKeys stripped). Raw JSON object. ", alias="providerConfig")
    public_ip: Optional[StrictStr] = Field(default=None, alias="publicIp")
    cluster_id: Optional[StrictStr] = Field(default=None, alias="clusterId")
    region: Optional[StrictStr] = None
    image_id: Optional[StrictStr] = Field(default=None, alias="imageId")
    image_ref: Optional[StrictStr] = Field(default=None, alias="imageRef")
    is_trial: Optional[StrictBool] = Field(default=None, alias="isTrial")
    expires_at: Optional[datetime] = Field(default=None, alias="expiresAt")
    error_message: Optional[StrictStr] = Field(default=None, alias="errorMessage")
    system_prompt: Optional[StrictStr] = Field(default=None, alias="systemPrompt")
    mcp_servers: Optional[Dict[str, Any]] = Field(default=None, alias="mcpServers")
    started_at: Optional[datetime] = Field(default=None, alias="startedAt")
    stopped_at: Optional[datetime] = Field(default=None, alias="stoppedAt")
    status_entered_at: Optional[datetime] = Field(default=None, alias="statusEnteredAt")
    connectivity_updated_at: Optional[datetime] = Field(default=None, alias="connectivityUpdatedAt")
    ms_connection_status: Optional[StrictStr] = Field(default=None, description="Independent MessageClient/Centrifugo connection reported by the current Runtime. One of `unknown` | `connecting` | `connected` | `reconnecting` | `disconnected` | `failed`. Cluster returns `unknown` when the Runtime lease is missing, expired, or the instance is stopped/terminated. This does not replace `connectivity`. ", alias="msConnectionStatus")
    ms_connection_updated_at: Optional[datetime] = Field(default=None, alias="msConnectionUpdatedAt")
    created_at: Optional[datetime] = Field(default=None, alias="createdAt")
    updated_at: Optional[datetime] = Field(default=None, alias="updatedAt")
    screenshot_url: Optional[StrictStr] = Field(default=None, alias="screenshotUrl")
    screenshot_updated_at: Optional[datetime] = Field(default=None, alias="screenshotUpdatedAt")
    capabilities: Optional[Dict[str, Any]] = Field(default=None, description="Frozen capability declaration snapshot (raw JSON object, nullable). Byte-for-byte passthrough of the stored `capabilities_json` (proto `InstanceData` field 98). Omitted entirely when the instance has no declared capabilities (pre-migration rows / no matching billing template) rather than emitted as `null`. Mirrors the same field on the main Gateway DTO (services/gateway/internal/dto/instance_data.go).  Note: this is the **instance-level** capability JSON object and is semantically distinct from `DeviceInfoDTO.capabilities` (a string array of device hardware / Agent-reported capabilities returned by `GET /api/v1/instances/{id}/device/info`). ")
    __properties: ClassVar[List[str]] = ["id", "ownerId", "agentFramework", "providerId", "externalId", "osType", "hostingType", "cloudProvider", "name", "modelPrimary", "models", "status", "desiredStatus", "endpoint", "bridgeId", "identityId", "totalRunSeconds", "connectivity", "connectivityReason", "haltReason", "providerConfig", "publicIp", "clusterId", "region", "imageId", "imageRef", "isTrial", "expiresAt", "errorMessage", "systemPrompt", "mcpServers", "startedAt", "stoppedAt", "statusEnteredAt", "connectivityUpdatedAt", "msConnectionStatus", "msConnectionUpdatedAt", "createdAt", "updatedAt", "screenshotUrl", "screenshotUpdatedAt", "capabilities"]

    @field_validator('ms_connection_status')
    def ms_connection_status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['unknown', 'connecting', 'connected', 'reconnecting', 'disconnected', 'failed']):
            raise ValueError("must be one of enum values ('unknown', 'connecting', 'connected', 'reconnecting', 'disconnected', 'failed')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of InstanceDataDTO from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of InstanceDataDTO from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "ownerId": obj.get("ownerId"),
            "agentFramework": obj.get("agentFramework"),
            "providerId": obj.get("providerId"),
            "externalId": obj.get("externalId"),
            "osType": obj.get("osType"),
            "hostingType": obj.get("hostingType"),
            "cloudProvider": obj.get("cloudProvider"),
            "name": obj.get("name"),
            "modelPrimary": obj.get("modelPrimary"),
            "models": obj.get("models"),
            "status": obj.get("status"),
            "desiredStatus": obj.get("desiredStatus"),
            "endpoint": obj.get("endpoint"),
            "bridgeId": obj.get("bridgeId"),
            "identityId": obj.get("identityId"),
            "totalRunSeconds": obj.get("totalRunSeconds"),
            "connectivity": obj.get("connectivity"),
            "connectivityReason": obj.get("connectivityReason"),
            "haltReason": obj.get("haltReason"),
            "providerConfig": obj.get("providerConfig"),
            "publicIp": obj.get("publicIp"),
            "clusterId": obj.get("clusterId"),
            "region": obj.get("region"),
            "imageId": obj.get("imageId"),
            "imageRef": obj.get("imageRef"),
            "isTrial": obj.get("isTrial"),
            "expiresAt": obj.get("expiresAt"),
            "errorMessage": obj.get("errorMessage"),
            "systemPrompt": obj.get("systemPrompt"),
            "mcpServers": obj.get("mcpServers"),
            "startedAt": obj.get("startedAt"),
            "stoppedAt": obj.get("stoppedAt"),
            "statusEnteredAt": obj.get("statusEnteredAt"),
            "connectivityUpdatedAt": obj.get("connectivityUpdatedAt"),
            "msConnectionStatus": obj.get("msConnectionStatus"),
            "msConnectionUpdatedAt": obj.get("msConnectionUpdatedAt"),
            "createdAt": obj.get("createdAt"),
            "updatedAt": obj.get("updatedAt"),
            "screenshotUrl": obj.get("screenshotUrl"),
            "screenshotUpdatedAt": obj.get("screenshotUpdatedAt"),
            "capabilities": obj.get("capabilities")
        })
        return _obj


